Your recorded letter sounds live in this folder.

Record them with the Sound Studio: start serve.bat on the computer, then open
http://localhost:8000/record.html

Each sound is one file (b.webm, sh.webm, ...) listed in manifest.json.
The game plays them on every device; deleting a file and re-saving the
manifest from the Studio removes a sound.
